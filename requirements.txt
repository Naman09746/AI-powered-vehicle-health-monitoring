# ──────────────────────────────────────────────
# Vehicle Health Monitor — Python Dependencies
# Extras group: [dev], [ml], [prod]
# ──────────────────────────────────────────────

# --- Core ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.14.2
python-multipart>=0.0.7

# --- Database ---
sqlalchemy>=2.0.51
asyncpg>=0.29.0
aiosqlite>=0.22.1
alembic>=1.13.0
psycopg2-binary>=2.9.0

# --- Auth ---
bcrypt>=4.1.0
python-jose[cryptography]>=3.5.0

# --- Async task queue ---
celery>=5.3.0
redis>=8.0.1

# --- ML ---
scikit-learn>=1.9.0
xgboost>=2.0.0
joblib>=1.5.3
shap>=0.45.0
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.11.1
plotly>=5.20.0
mlflow>=2.12.0
boto3>=1.34.0

# --- API middleware ---
slowapi>=0.1.9
structlog>=24.2.0

# --- MQTT ---
paho-mqtt>=2.1.0

# --- Reporting ---
reportlab>=4.1.0

# --- Web Push ---
pywebpush>=1.14.0

# --- Env / Config ---
python-dotenv>=1.2.2

# ── Dev dependencies (pip install -e ".[dev]") ──
# ruff, mypy, pytest, pre-commit live here
# See pyproject.toml for dev extras
